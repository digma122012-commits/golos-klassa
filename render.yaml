services:
  - type: web
    name: golos-klassa
    env: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python app.py"
    envVars:
      - key: PORT
        value: 10000
      - key: ADMIN_PASSWORD
        value: school123!  # ← ОБЯЗАТЕЛЬНО измени в Render!
      - key: SECRET_KEY
        generateValue: true